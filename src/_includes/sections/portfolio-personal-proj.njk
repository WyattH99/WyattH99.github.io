
<section class="bg-zinc-900 w-full text-white text-center px-4 py-4 md:px-8 flex flex-col items-center">
    
    {% import "sections/sectiontitle-macro.njk" as macro %}
    {{ macro.sectiontitle("Personal Projects") }}

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-5xl mx-auto mt-8">
        {% import "sections/sectioncards-macro.njk" as macro %}
        {{ macro.sectioncards("./assets/Head-BW.jpeg", "Sofle Keyboard", "Successfully did stuff that sounds impressive with quantified results.") }}
        {{ macro.sectioncards("./assets/Head-BW.jpeg", "Sterling Engine", "Successfully did stuff that sounds impressive with quantified results.") }}
        {{ macro.sectioncards("./assets/Head-BW.jpeg", "Pneumatic Engine", "Successfully did stuff that sounds impressive with quantified results.") }}
        {{ macro.sectioncards("./assets/Head-BW.jpeg", "3D Printed Drone", "Successfully did stuff that sounds impressive with quantified results.") }}
        {{ macro.sectioncards("./assets/Head-BW.jpeg", "Pecky Cypress Epoxy Pebble Foyer Table", "Successfully did stuff that sounds impressive with quantified results.") }}
        {{ macro.sectioncards("./assets/Head-BW.jpeg", "3D Printers", "Successfully did stuff that sounds impressive with quantified results.") }}
    </div>
    
</section>